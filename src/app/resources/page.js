import ResourcesClient from '@/components/ResourcesClient'

export const metadata = {
  title: 'Founder Resources — LaunchLog',
  description: 'Curated funding, legal, finance, and growth resources for early-stage founders. Organized by startup type.',
}

export default function ResourcesPage() {
  return <ResourcesClient />
}
