import ListClient from '@/components/ListClient'

export const metadata = {
  title: 'List Your Startup',
  description: 'Get your early-stage startup in front of real people who care about new products. Free to list, no pitch deck required.',
}

export default function ListPage() {
  return <ListClient />
}
